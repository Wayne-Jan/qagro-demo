# q_agro Evidence Demo

Public frontend for the q_agro evidence webmap.

The repository intentionally contains only the static UI. Runtime data is loaded from the configured data endpoint and is not committed here.
